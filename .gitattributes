# Enforce consistent line endings for text files
# Use LF in the repository for text files to avoid cross-platform warnings
* text=auto

# Explicit rules for common text formats in this repo
*.py text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.tsv text eol=lf

# Treat vector embedding files as binary to avoid unwanted conversions
*.vec -text
